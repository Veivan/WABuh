package helper;

public class TokenSearchResult {
	   public String token;
	   public boolean isFound;

	   public void SearchResult(String token, boolean isFound) {
	      this.token = token;
	      this.isFound = isFound;
	   }
}
